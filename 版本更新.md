# 版本更新操作说明

本文档说明如何把 `collapse_score_lite.py` 更新发布到 [PyPI](https://pypi.org/project/collapse-score-lite/)（包名 `collapse-score-lite`）。

> 适用环境：Windows + PowerShell + Python 3.8 及以上。

---

## 一、目录结构说明

```
yl-causal-chain/
├─ collapse_score_lite.py      # ★ 源文件（日常修改的就是它）
├─ packaging/                  # 打包目录（发布时使用）
│  ├─ collapse_score_lite.py   # 模块副本（必须与源文件保持一致）
│  ├─ pyproject.toml           # 包元数据与版本号
│  ├─ README.md                # 包说明（PyPI 页面展示内容）
│  ├─ dist/                    # 构建产物（可整体删除）
│  ├─ build/                   # 构建中间产物（可整体删除）
│  └─ collapse_score_lite.egg-info/  # 构建中间产物（可整体删除）
└─ 更新到github-更新.bat        # 推送到 GitHub 的脚本
```

**关键点**：`packaging/collapse_score_lite.py` 只是源文件的**副本**，每次改完源文件后，**必须先同步到 packaging 目录**再构建，否则发布的是旧代码。

---

## 二、首次准备（只需做一次）

1. **确认 Python 版本**

   ```powershell
   python --version   # 需 >= 3.8
   ```

2. **安装打包与上传工具**

   ```powershell
   python -m pip install build twine
   ```

3. **准备 PyPI API Token**

   在 [PyPI 账户设置 → API tokens](https://pypi.org/manage/account/token/) 创建一个 token（权限勾选项目 `collapse-score-lite`，或 “Entire account”），复制备用。token 形如 `pypi-xxxx...`。

---

## 三、标准更新流程（8 步）

### 第 1 步：修改源文件

编辑根目录下的 `collapse_score_lite.py`，完成功能修改。

### 第 2 步：同步模块到 packaging 目录

```powershell
Copy-Item 'x:\release\yl-causal-chain\collapse_score_lite.py' 'x:\release\yl-causal-chain\packaging\collapse_score_lite.py' -Force
```

### 第 3 步：递增版本号

打开 `packaging/pyproject.toml`，把 `version` 加一：

```toml
[project]
name = "collapse-score-lite"
version = "0.1.2"        # ← 由 0.1.1 改为 0.1.2
```

> ⚠️ **PyPI 不允许覆盖同名文件**。即使只改了一行代码，也必须递增版本号，否则会报 `400 File already exists`。

### 第 4 步：（可选）更新包说明

若接口、用法有变化，同步更新 `packaging/README.md`。

### 第 5 步：清理旧的构建产物

```powershell
cd 'x:\release\yl-causal-chain\packaging'
Remove-Item -Recurse -Force dist, build, collapse_score_lite.egg-info -ErrorAction SilentlyContinue
```

### 第 6 步：构建

```powershell
python -m build
```

构建成功后 `dist/` 下会生成两个文件：

```
collapse_score_lite-0.1.2-py3-none-any.whl   # wheel
collapse_score_lite-0.1.2.tar.gz             # sdist
```

### 第 7 步：校验产物

```powershell
python -m twine check dist/*
```

看到两个 `PASSED` 即为通过。

### 第 8 步：上传到 PyPI

```powershell
$env:TWINE_USERNAME = '__token__'
$env:TWINE_PASSWORD = 'pypi-你的Token'
$env:PYTHONUTF8 = '1'
$env:PYTHONIOENCODING = 'utf-8'
python -m twine upload --non-interactive --disable-progress-bar dist/*
```

> 把 `pypi-你的Token` 替换成真实的 token。成功后会打印 `View at: https://pypi.org/project/collapse-score-lite/0.1.2/`。

验证是否发布成功：

```powershell
python -m pip index versions collapse-score-lite
```

---

## 四、常见问题与排查

| 现象 | 原因 | 解决办法 |
|:--|:--|:--|
| `400 File already exists` | 版本号重复，PyPI 不允许覆盖同名文件 | 递增 `pyproject.toml` 中的 `version` |
| `UnicodeEncodeError: 'gbk' codec can't encode...` | Windows 终端 GBK 编码无法渲染进度条 | 加 `--disable-progress-bar`，并设 `$env:PYTHONUTF8='1'` |
| 发布后功能没变 | 忘了同步 `collapse_score_lite.py` 到 `packaging/` | 重新执行第 2 步后再构建上传 |
| `403 Forbidden` | token 无该项目权限 / 已失效 | 重新创建 token，确认勾选对应项目权限 |
| `twine check` 报错 | 元数据格式问题 | 检查 `pyproject.toml` 语法与 `description` 等字段 |

---

## 五、注意事项

1. **Token 安全**：token 拥有发布权限，勿提交到 git、勿截图外发；泄露后请在 PyPI 后台立即撤销并重建。
2. **不可覆盖**：PyPI 上同一文件名只能上传一次，任何改动都需新版本号。
3. **版本号规范**：建议遵循语义化版本 `主.次.修订`（如 `0.1.2`）。
4. **先构建后上传**：确保 `python -m build` 在 `twine check` 之前完成，且 `dist/` 下只有本次要发布的文件。
